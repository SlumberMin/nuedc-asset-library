# 嵌入式视觉前沿技术

## 1. 模型压缩技术

### 1.1 知识蒸馏
```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class DistillationLoss(nn.Module):
    def __init__(self, temperature=4.0, alpha=0.7):
        super().__init__()
        self.T = temperature
        self.alpha = alpha
        self.ce_loss = nn.CrossEntropyLoss()
    
    def forward(self, student_logits, teacher_logits, labels):
        # 硬标签损失
        hard_loss = self.ce_loss(student_logits, labels)
        
        # 软标签损失 (KL散度)
        soft_student = F.log_softmax(student_logits / self.T, dim=1)
        soft_teacher = F.softmax(teacher_logits / self.T, dim=1)
        soft_loss = F.kl_div(soft_student, soft_teacher, reduction='batchmean')
        soft_loss *= self.T ** 2
        
        return self.alpha * soft_loss + (1 - self.alpha) * hard_loss
```

### 1.2 剪枝 (Pruning)
```python
import torch.nn.utils.prune as prune

# 非结构化剪枝 - L1范数
for name, module in model.named_modules():
    if isinstance(module, nn.Conv2d):
        prune.l1_unstructured(module, name='weight', amount=0.3)

# 结构化剪枝 - 通道剪枝
for name, module in model.named_modules():
    if isinstance(module, nn.Conv2d):
        prune.ln_structured(module, name='weight', amount=0.2, n=1, dim=0)

# 移除剪枝mask，使剪枝永久化
for name, module in model.named_modules():
    if isinstance(module, nn.Conv2d):
        prune.remove(module, 'weight')
```

### 1.3 低秩分解
```python
def decompose_conv(conv_layer, rank_ratio=0.5):
    """将大卷积分解为两个小卷积"""
    W = conv_layer.weight.data  # [out, in, kH, kW]
    out_ch, in_ch, kH, kW = W.shape
    rank = int(min(out_ch, in_ch) * rank_ratio)
    
    # SVD 分解
    W_2d = W.reshape(out_ch, -1)
    U, S, Vt = torch.linalg.svd(W_2d, full_matrices=False)
    
    # 低秩近似
    A = U[:, :rank] * S[:rank].sqrt()      # [out_ch, rank]
    B = Vt[:rank, :].reshape(rank, in_ch, kH, kW)  # [rank, in, kH, kW]
    
    return A, B
```

---

## 2. 量化技术

### 2.1 训练后量化 (PTQ)
```python
import torch.quantization as quant

# 动态量化
model_dynamic = torch.quantization.quantize_dynamic(
    model, {nn.Linear, nn.Conv2d}, dtype=torch.qint8
)

# 静态量化
model.qconfig = quant.get_default_qconfig('qnnpack')
model_prepared = quant.prepare(model)
# 校准
for data in calibration_loader:
    model_prepared(data)
model_quantized = quant.convert(model_prepared)
```

### 2.2 量化感知训练 (QAT)
```python
# QAT 流程
model.qconfig = quant.get_default_qat_qconfig('qnnpack')
model_prepared = quant.prepare_qat(model)

# 训练
for epoch in range(num_epochs):
    for data, target in train_loader:
        output = model_prepared(data)
        loss = criterion(output, target)
        loss.backward()
        optimizer.step()

model_quantized = quant.convert(model_prepared.eval())
```

### 2.3 混合精度量化
```python
# 敏感层保持高精度
def get_layer_qconfig(layer_name, sensitivity):
    if sensitivity > threshold:
        return quant.float16_qconfig  # FP16
    return quant.get_default_qconfig('qnnpack')  # INT8

# 逐层配置
for name, module in model.named_modules():
    module.qconfig = get_layer_qconfig(name, sensitivity_map[name])
```

### 2.4 量化工具对比
| 工具 | 平台 | 支持格式 | 特点 |
|------|------|----------|------|
| ONNX Runtime | 通用 | INT8/FP16 | 跨平台 |
| TensorRT | NVIDIA | INT8/FP16/INT4 | GPU最优 |
| RKNN Toolkit | RK3588 | INT8/FP16 | NPU原生 |
| TFLite | 移动端 | INT8/FP16 | 生态完善 |
| NCNN | 移动端 | INT8 | 轻量高效 |
| PaddleLite | 移动端 | INT8 | 百度生态 |

---

## 3. 轻量级网络设计

### 3.1 深度可分离卷积
```python
class DepthwiseSeparable(nn.Module):
    def __init__(self, in_ch, out_ch, stride=1):
        super().__init__()
        self.dw = nn.Conv2d(in_ch, in_ch, 3, stride, 1, groups=in_ch, bias=False)
        self.bn1 = nn.BatchNorm2d(in_ch)
        self.pw = nn.Conv2d(in_ch, out_ch, 1, bias=False)
        self.bn2 = nn.BatchNorm2d(out_ch)
        self.act = nn.ReLU6(inplace=True)
    
    def forward(self, x):
        x = self.act(self.bn1(self.dw(x)))
        x = self.act(self.bn2(self.pw(x)))
        return x
```

### 3.2 Ghost Module (华为)
```python
class GhostModule(nn.Module):
    """用线性变换生成更多特征图，减少计算量"""
    def __init__(self, in_ch, out_ch, ratio=2):
        super().__init__()
        init_ch = out_ch // ratio
        new_ch = out_ch - init_ch
        
        self.primary = nn.Sequential(
            nn.Conv2d(in_ch, init_ch, 1, bias=False),
            nn.BatchNorm2d(init_ch),
            nn.ReLU(inplace=True)
        )
        self.cheap = nn.Sequential(
            nn.Conv2d(init_ch, new_ch, 3, 1, 1, groups=init_ch, bias=False),
            nn.BatchNorm2d(new_ch),
            nn.ReLU(inplace=True)
        )
    
    def forward(self, x):
        y = self.primary(x)
        z = self.cheap(y)
        return torch.cat([y, z], dim=1)
```

### 3.3 ShuffleNet v2 通道重排
```python
def channel_shuffle(x, groups):
    B, C, H, W = x.shape
    x = x.reshape(B, groups, C // groups, H, W)
    x = x.permute(0, 2, 1, 3, 4)
    return x.reshape(B, C, H, W)
```

---

## 4. 目标检测轻量化

### 4.1 轻量级检测头
```python
class LiteDetect(nn.Module):
    """轻量级检测头"""
    def __init__(self, in_ch_list, num_classes, anchors):
        super().__init__()
        self.convs = nn.ModuleList([
            nn.Sequential(
                nn.Conv2d(ch, ch, 3, 1, 1, groups=ch, bias=False),
                nn.BatchNorm2d(ch),
                nn.SiLU(),
                nn.Conv2d(ch, len(anchors) * (5 + num_classes), 1)
            ) for ch in in_ch_list
        ])
```

### 4.2 Anchor-Free 方案
- **CenterNet**: 基于中心点检测，无需 NMS
- **FCOS**: 逐像素预测，结构简单
- **NanoDet**: 超轻量，适合边缘设备

### 4.3 NPU 优化检测模型
```python
# 导出为 RKNN 适配格式
class NPUFriendlyDetect(nn.Module):
    def __init__(self):
        super().__init__()
        # 避免动态shape操作
        # 使用固定大小的 reshape
        # 避免不支持的算子（如 deformable conv）
```

---

## 5. 边缘计算框架

### 5.1 框架对比
| 框架 | 推理速度 | 模型支持 | 部署难度 |
|------|----------|----------|----------|
| NCNN | ★★★★★ | ONNX | 低 |
| MNN | ★★★★ | ONNX/TFLite | 低 |
| TNN | ★★★★ | ONNX | 低 |
| TFLite | ★★★ | TFLite | 中 |
| RKNN | ★★★★★ | ONNX/TF/PT | 中 |
| ONNX RT | ★★★★ | ONNX | 低 |

### 5.2 NCNN 部署示例
```cpp
#include <ncnn/net.h>

ncnn::Net net;
net.opt.use_vulkan_compute = true;  // GPU加速
net.load_param("model.param");
net.load_model("model.bin");

ncnn::Mat in = ncnn::Mat::from_pixels_resize(bgr, ncnn::Mat::PIXEL_BGR2RGB, w, h, 224, 224);
in.substract_mean_normalize(mean_vals, norm_vals);

ncnn::Extractor ex = net.create_extractor();
ex.input("input", in);
ex.extract("output", out);
```

### 5.3 TNN 部署
```cpp
#include <tnn/core/tnn.h>
#include <tnn/core/instance.h>

auto tnn = std::make_shared<TNN::TNN>();
tnn->Init(config);

auto instance = tnn->CreateInstance();
TNN::MatConvertParam input_param;
instance->SetInputMat(input_mat, input_param);

TNN::MatConvertParam output_param;
instance->GetOutputMat(output_mat, output_param);
```

---

## 6. 实时语义分割

### 6.1 BiSeNet 架构
- 空间路径：保持高分辨率空间信息
- 上下文路径：快速获取语义信息
- 双路径融合，实现实时推理

### 6.2 PP-LiteSeg (百度)
```python
class PPLiteSeg(nn.Module):
    """超轻量语义分割"""
    def __init__(self, num_classes):
        super().__init__()
        self.backbone = STDCNet()  # 轻量骨干
        self.sppm = SPPM()        # 空间金字塔池化
        self.decoder = SSLD()     # 融合解码器
```

### 6.3 量化后精度对比
| 模型 | FP32 mIoU | INT8 mIoU | FPS (RK3588) |
|------|-----------|-----------|--------------|
| BiSeNetV2 | 73.4 | 72.1 | 45 |
| PP-LiteSeg-T | 72.0 | 71.2 | 68 |
| STDC1 | 71.9 | 70.8 | 72 |

---

## 7. 多模态融合

### 7.1 RGB-D 融合
```python
class RGBDFusion(nn.Module):
    def __init__(self):
        super().__init__()
        self.rgb_branch = MobileNetV3()
        self.depth_branch = LiteDepthNet()
        self.fusion = CrossAttention(dim=256)
    
    def forward(self, rgb, depth):
        rgb_feat = self.rgb_branch(rgb)
        depth_feat = self.depth_branch(depth)
        fused = self.fusion(rgb_feat, depth_feat)
        return fused
```

### 7.2 超声波+视觉融合
```
视觉检测: 目标位置 (x, y)
超声波测距: 目标距离 d
融合结果: 3D 坐标 (x * d/fx, y * d/fy, d)
```

---

## 8. 前沿研究方向

### 8.1 神经架构搜索 (NAS)
- 自动搜索最优网络结构
- 硬件感知 NAS：针对特定硬件约束
- One-Shot NAS：单次训练，搜索多种架构

### 8.2 自监督预训练
- MAE (Masked Autoencoder)
- SimCLR / MoCo 对比学习
- 减少标注数据需求

### 8.3 端侧大模型
- MobileLLM (Meta)
- Phi-3 Mini (Microsoft)
- TinyLlama (1.1B)

### 8.4 脉冲神经网络 (SNN)
- 超低功耗，适合电池供电场景
- Intel Loihi、IBM TrueNorth 等专用芯片
- 精度逐步逼近 ANN

---

## 9. 电赛应用建议

### 9.1 快速上手路线
```
1. NCNN/TFLite 学习 → 2. 量化工具使用 → 3. 轻量模型部署 → 4. 系统集成优化
```

### 9.2 推荐模型
| 任务 | 推荐模型 | 精度 | 速度 |
|------|----------|------|------|
| 分类 | MobileNetV3-Small | 67.5% | 1ms |
| 检测 | NanoDet-Plus | 27.0mAP | 3ms |
| 分割 | PP-LiteSeg-T | 72.0mIoU | 8ms |
| 姿态 | MoveNet-Lightning | 70AP | 4ms |
| OCR | PPOCRv4-mobile | 90%+ | 5ms |

---

## 10. 参考资源

| 资源 | 链接 |
|------|------|
| NCNN | https://github.com/Tencent/ncnn |
| MNN | https://github.com/alibaba/MNN |
| TNN | https://github.com/Tencent/TNN |
| PaddleLite | https://github.com/PaddlePaddle/Paddle-Lite |
| RKNN Model Zoo | https://github.com/airockchip/rknn_model_zoo |
