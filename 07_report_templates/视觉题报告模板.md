# 视觉类题目 — 设计报告模板

> 适用于目标识别、循迹、颜色分拣、字符识别、测距等视觉/图像处理类赛题  
> 撰写人：____  |  日期：____年____月____日  |  赛题编号：____

---

## 一、系统方案设计

### 1.1 题目分析

| 指标项 | 题目要求 | 设计目标值 |
|--------|---------|-----------|
| 识别准确率 | ≥____% | ____% |
| 识别速度 | ≤____ms/帧 | ____ms/帧 |
| 分辨率要求 | ____×____ | ____×____ |
| 识别距离范围 | ____~____cm | ____~____cm |

### 1.2 总体方案框图

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 图像采集  │───▶│ 预处理    │───▶│ 特征提取  │───▶│ 识别/决策 │
│ (摄像头)  │    │ (滤波/二值化)│   │ (形状/颜色) │   │ (分类器)  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                    │
                                                    ▼
                                             ┌──────────┐
                                             │ 执行机构  │
                                             │ (舵机/电机)│
                                             └──────────┘
```

### 1.3 摄像头选型

| 方案 | 型号 | 分辨率 | 帧率 | 接口 | 优点 | 缺点 |
|------|------|--------|------|------|------|------|
| 方案一 | OpenMV | | | UART | | |
| 方案二 | OV2640+STM32 | | | DCMI | | |
| 方案三 | K210 | | | UART | | |
| **选择** | | | | | | |

---

## 二、图像处理算法设计

### 2.1 图像处理流程总览

```
原始图像 → 灰度化 → 滤波降噪 → 二值化 → 形态学处理 → 轮廓提取 → 特征计算 → 目标识别
```

### 2.2 图像预处理

#### 2.2.1 色彩空间转换

| 转换 | 公式/方法 | 用途 |
|------|---------|------|
| RGB → 灰度 | Y = 0.299R + 0.587G + 0.114B | 边缘检测 |
| RGB → HSV | OpenCV cvtColor | 颜色识别 |
| RGB → LAB | OpenCV cvtColor | 光照不变性 |

#### 2.2.2 滤波降噪

| 滤波方法 | 核大小 | 效果对比 | 是否采用 |
|---------|--------|---------|---------|
| 均值滤波 | 3×3 | 轻微模糊 | |
| 中值滤波 | 3×3 | 去椒盐噪声 | |
| 高斯滤波 | 3×3, σ=1.0 | 平滑效果好 | |
| 双边滤波 | | 保边去噪 | |

**滤波前后对比图：**
```
[粘贴原图]  →  [粘贴滤波后图]
```

#### 2.2.3 二值化

| 方法 | 参数 | 效果 | 是否采用 |
|------|------|------|---------|
| 固定阈值 | 阈值=____ | | |
| OTSU 自适应 | 自动 | | |
| 自适应阈值 | blockSize=__, C=__ | | |
| HSV 颜色阈值 | H:__-__, S:__-__, V:__-__ | | |

**HSV 颜色阈值标定记录（适用于颜色识别题）：**

| 目标颜色 | H_min | H_max | S_min | S_max | V_min | V_max |
|---------|-------|-------|-------|-------|-------|-------|
| 红色 | | | | | | |
| 绿色 | | | | | | |
| 蓝色 | | | | | | |
| 黄色 | | | | | | |
| 白色 | | | | | | |
| 黑色 | | | | | | |

**二值化效果对比：**
```
[粘贴二值化前后对比图]
```

#### 2.2.4 形态学处理

| 操作 | 核大小 | 次数 | 目的 |
|------|--------|------|------|
| 腐蚀 | 3×3 | __次 | 消除小噪点 |
| 膨胀 | 3×3 | __次 | 填充空洞 |
| 开运算 | 3×3 | __次 | 去除小物体 |
| 闭运算 | 3×3 | __次 | 连接断裂区域 |

### 2.3 目标检测与识别

#### 2.3.1 轮廓检测

```python
contours, hierarchy = cv2.findContours(binary, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
```

**轮廓筛选条件：**

| 筛选条件 | 阈值 | 目的 |
|---------|------|------|
| 面积范围 | ____~____ 像素 | 过滤噪声 |
| 长宽比 | ____~____ | 区分形状 |
| 圆度 | ≥____ | 区分圆形/方形 |
| 占空比 | ____~____ | 区分目标 |

#### 2.3.2 形状识别算法

**基于 Hu 矩 / 凸包 / 多边形拟合：**

| 形状 | 判据 | 识别准确率 |
|------|------|-----------|
| 三角形 | 边数=3 | ____% |
| 正方形 | 边数=4, 长宽比≈1 | ____% |
| 长方形 | 边数=4, 长宽比≠1 | ____% |
| 圆形 | 圆度≥0.85 | ____% |

#### 2.3.3 颜色识别

```python
# HSV 颜色识别核心代码
hsv = cv2.cvtColor(roi, cv2.COLOR_BGR2HSV)
mask = cv2.inRange(hsv, lower_bound, upper_bound)
```

#### 2.3.4 字符识别（OCR）

| 方案 | 方法 | 准确率 | 速度 |
|------|------|--------|------|
| 模板匹配 | cv2.matchTemplate | | |
| 特征点匹配 | ORB/SIFT | | |
| KNN 分类 | 轮廓特征+KNN | | |
| CNN | 轻量级网络 | | |

### 2.4 目标定位与测距

#### 2.4.1 像素坐标到物理坐标映射

```
[粘贴标定图像]
```

**相机标定参数：**

| 参数 | 数值 |
|------|------|
| 焦距 fx | ____px |
| 焦距 fy | ____px |
| 光心 cx | ____px |
| 光心 cy | ____px |
| 畸变系数 | |

#### 2.4.2 测距方法

| 方法 | 公式 | 精度 |
|------|------|------|
| 单目测距 | D = H×f / h | |
| 双目测距 | D = B×f / d | |
| 激光辅助 | 直接测量 | |

**测距标定数据：**

| 实际距离(cm) | 像素宽度(px) | 计算距离(cm) | 误差(cm) | 误差率 |
|-------------|-------------|-------------|---------|--------|
| 20 | | | | |
| 30 | | | | |
| 50 | | | | |
| 80 | | | | |
| 100 | | | | |

---

## 三、关键算法实现

### 3.1 OpenMV / K210 实现

```python
# 帧率优化策略
sensor.set_pixformat(sensor.RGB565)
sensor.set_framesize(sensor.QVGA)  # 320x240
sensor.skip_frames(time=2000)
clock = time.clock()

while True:
    clock.tick()
    img = sensor.snapshot()

    # 颜色阈值查找（调试用）
    # thresholds = [(30, 100, 15, 127, 15, 127)]

    blobs = img.find_blobs(thresholds, pixels_threshold=100, area_threshold=100)
    for blob in blobs:
        if blob.roundness() > 0.7:  # 圆度筛选
            img.draw_rectangle(blob.rect())
            img.draw_cross(blob.cx(), blob.cy())

    print(clock.fps())
```

### 3.2 OpenCV + STM32 / 树莓派实现

```python
import cv2
import numpy as np

def process_frame(frame):
    # 1. 预处理
    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
    blurred = cv2.GaussianBlur(gray, (5, 5), 0)

    # 2. 二值化
    _, binary = cv2.threshold(blurred, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)

    # 3. 形态学
    kernel = cv2.getStructuringElement(cv2.MORPH_RECT, (3, 3))
    binary = cv2.morphologyEx(binary, cv2.MORPH_CLOSE, kernel, iterations=2)

    # 4. 轮廓检测
    contours, _ = cv2.findContours(binary, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)

    results = []
    for cnt in contours:
        area = cv2.contourArea(cnt)
        if area < 500:
            continue

        # 形状判断
        peri = cv2.arcLength(cnt, True)
        approx = cv2.approxPolyDP(cnt, 0.04 * peri, True)
        vertices = len(approx)

        # 最小外接矩形
        rect = cv2.minAreaRect(cnt)
        w, h = rect[1]
        aspect_ratio = max(w, h) / (min(w, h) + 1e-6)

        results.append({
            'center': tuple(map(int, cv2.minEnclosingCircle(cnt)[0])),
            'area': area,
            'vertices': vertices,
            'aspect_ratio': aspect_ratio
        })

    return results
```

---

## 四、硬件设计

### 4.1 摄像头参数配置

| 参数 | 设置值 | 说明 |
|------|--------|------|
| 分辨率 | ____×____ | |
| 帧率 | ____fps | |
| 曝光 | ____ | 自动/手动 |
| 白平衡 | | |
| 增益 | | |

### 4.2 光源设计

| 项目 | 方案 | 说明 |
|------|------|------|
| 补光灯 | LED 环形灯 | |
| 色温 | ____K | |
| 照射角度 | ____° | |
| 均匀性 | ____ | |

### 4.3 机械结构

```
[粘贴摄像头安装位置示意图]
[粘贴实物照片]
```

---

## 五、测试方案与结果

### 5.1 不同光照条件测试

| 光照条件 | 识别率 | 误识别率 | 说明 |
|---------|--------|---------|------|
| 强光 | ____% | ____% | |
| 正常光照 | ____% | ____% | |
| 弱光 | ____% | ____% | |
| 逆光 | ____% | ____% | |

### 5.2 不同距离测试

| 距离(cm) | 识别率 | 识别时间(ms) | 位置误差(px) |
|---------|--------|-------------|-------------|
| 20 | | | |
| 50 | | | |
| 100 | | | |
| 150 | | | |

### 5.3 不同角度测试

| 偏转角度 | 识别率 | 说明 |
|---------|--------|------|
| 0° | | 正面 |
| 15° | | |
| 30° | | |
| 45° | | |

### 5.4 速度测试

| 测试项 | 指标 |
|--------|------|
| 单帧处理时间 | ____ms |
| 平均帧率 | ____fps |
| 识别延迟 | ____ms |
| 从采集到输出总延迟 | ____ms |

### 5.5 指标达成汇总

| 指标 | 题目要求 | 实测值 | 是否达标 |
|------|---------|--------|---------|
| | | | |

### 5.6 典型识别结果展示

```
[粘贴多组典型识别结果图]
[标注：原图 → 处理中间结果 → 最终识别标注]
```

---

## 六、总结

### 6.1 创新点
1. 
2. 

### 6.2 不足与改进
1. 
2. 

### 6.3 心得体会

---

## 附录

### 附录 A：完整源代码
### 附录 B：颜色阈值参数表
### 附录 C：测试视频截图
### 附录 D：元件清单
